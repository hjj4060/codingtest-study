링크 : https://www.inflearn.com/course/코딩테스트-자바-개념/

String
    1. charAt(I) => 문자열의 위치
    2. charAt(i) -‘0’ => int 값
    3. toCharArray() => char[] return
    4. indexOf() => 문자의 위치를 반환
    5. subString() => substirng(0, 7) = teacher
    6. Palindrome => 가운데 기준으로 양사이드값 같아야함
        - [ ] Ex) banana=>anana


